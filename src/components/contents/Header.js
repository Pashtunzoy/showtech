import React, { PropTypes } from 'react';
import { Link } from 'react-router';

const Header = (props) => {
  return (
    <div>
      <Link to="/">Home</Link>
    </div>
  )
}

export default Header;
